#!/usr/bin/env decaf

// Inputs ints and prints :) until a 0 is entered

class Aaa {
    public static void main (String[] argv) {
        IO.putString("Enter ints, 0 to exit.\n");
        while (IO.getInt()) {
            IO.putString(":)\n");
        }
        IO.putString(":(\n");
    }
}