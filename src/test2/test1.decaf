#!/usr/bin/env decaf

// Inputs ints and prints :) until a 0 is entered

class Aaa {
    public static void main (String[] argv) {
        while (IO.getInt()) {
            IO.putString(":)");
            IO.putChar('\n');
        }
        IO.putString(":(");
        IO.putChar('\n');
    }
}