#!/usr/bin/env decaf

class Aaa {
    // public static void main (String[] argv) {
    public static void main (int a, int b) {
        IO.putString("x = ");
        int x = IO.getInt();
        if (x%2) {
            IO.putInt(x);
            IO.putString(" is odd");
        } else {
            IO.putInt(x);
            IO.putString(" is even");
        }
        IO.putChar('\n');
    }
}