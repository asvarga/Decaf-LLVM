#!/usr/bin/env decaf

class Aaa {
    public static void main (String[] argv) {
        IO.putString("x = ");
        int x = IO.getInt();
        if (x%2) {
            IO.putInt(x);
            IO.putString(" is odd\n");
        } else {
            IO.putInt(x);
            IO.putString(" is even\n");
        }
    }
}