#!/usr/bin/env decaf

// method tests

class Aaa {
    public static void main (String[] argv) {
        putPairLn(1, 10);
        putPairLn(2, 10);
        putPairLn(3, 10);
    }

    void putPairLn(int x, int y) {
        IO.putChar('(');
        IO.putInt(x);
        IO.putString(", ");
        IO.putInt(y);
        IO.putChar(')');
        endl();
    }

    void endl() {
        IO.putChar('\n');
    }
}
