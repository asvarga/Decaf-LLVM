#!/usr/bin/env decaf

// method tests

class Aaa {
    public static void main (String[] argv) {
        Bbb.putPairLn(1, 10);
        Bbb.putPairLn(2, 10);
        Bbb.putPairLn(3, 10);
    }
}

class Bbb {
    void putPairLn(int x, int y) {
        IO.putChar('(');
        IO.putInt(x);
        IO.putString(", ");
        IO.putInt(y);
        IO.putChar(')');
        endl();
    }

    void endl() {
        IO.putChar('\n');
    }
}
