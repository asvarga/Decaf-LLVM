#!/usr/bin/env decaf

// string tests

class Aaa {
    public static void main () {
        IO.putString("My name is ");
        char c = IO.peek();
        string name = IO.getLine();
        IO.putString("Hello ");
        IO.putChar(c); IO.putChar('-');
        IO.putChar(c); IO.putChar('-');
        IO.putChar(c); IO.putChar('-');
        IO.putString(name);
    }
}