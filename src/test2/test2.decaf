#!/usr/bin/env decaf

// mutable variable tests

class Aaa {
    // public static void main (String[] argv) {
    public static void main () {
        IO.putString("x = ");
        int x = IO.getInt();
        if (x > 0) {        // TODO: while
            x = x-1;
            IO.putInt(x);
            IO.putChar('\n');
        }
    }
}