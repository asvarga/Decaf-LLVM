#!/usr/bin/env decaf

class Adder {
  // static public void main (String[] argv)
  public static void main () 
  {
    IO.putString ("enter two integers: ");
    int x = IO.getInt ();
    int y = IO.getInt ();

    IO.putInt (x);
    IO.putString (" + ");
    IO.putInt (y);
    IO.putString (" = ");
    IO.putInt (x+y);
    IO.putChar ('\n');
  }
}